name: webview_payment_resolver
description: >-
  Resolve WebView payment flows honestly with three states — paid, declined,
  unknown — covering abandoned flows, bank-app hand-offs, and missed callbacks.
version: 0.1.0
repository: https://github.com/Romana9/webview_payment_resolver
issue_tracker: https://github.com/Romana9/webview_payment_resolver/issues

environment:
  sdk: ^3.5.0

dev_dependencies:
  fake_async: ^1.3.1
  lints: ^5.0.0
  test: ^1.25.0
